==============================================================================

                        Vacui export internal directory

This directory is used as which store of pre-compiled  shared/static 
internal Vacui libraries, C/C++ headers into one place.

