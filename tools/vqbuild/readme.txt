                 VQBuild

VQBuild is a C/C++ build orchestrator, which is used by Vacui
source code. It searches for 'vqmodule.toml' file in specified directories
and compiles source files, collects their object files and links objects
to the target.