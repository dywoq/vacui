_______________________________________________________________________________

                            Base Services Layer

The Base Services Layer (BSL) is a freestanding collection of headers. It is 
used to make library code (e.g. filesystem parsing, reading and formatting 
logic) portable to a host operating system (where Vacui is developed) or Vacui.  
It prevents rewriting same logic twice. 
