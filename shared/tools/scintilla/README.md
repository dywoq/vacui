# Scintilla

The Scintilla is a command-line interface tool, used withing the Vacui operating system development.
Its goal is to handle the boilerplate tasks (such as building operating system, formatting the codebase etc.)
and simplify the development.

