# Scintilla

Scintilla is a command line interface tool, used within the Vacui operating system development.
Its goal is to handle the boilerplate tasks (such as building operating system, formatting the codebase, 
generating ISO images) and simplify  the development.

